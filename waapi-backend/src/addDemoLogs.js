/**
 * Demo logs have been disabled
 * Real-time logs are now being used instead
 */

console.log('Demo logs have been disabled.');
console.log('The application is now using real-time logs only.');
console.log('This script is kept as a placeholder to avoid breaking any references to it.');

// Exit with success code
process.exit(0); 